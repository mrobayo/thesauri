
{{ content() }}

<div class="jumbotron">
    <h1>Página no encontrada</h1>
    <p>Disculpa, has accesado a un página que no existe o fue movida.</p>
    <p>{{ link_to('index', 'Inicio', 'class': 'btn btn-primary') }}</p>
</div>
